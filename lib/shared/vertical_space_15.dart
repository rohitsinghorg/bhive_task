import 'package:bhive_task/core/core.dart';
import 'package:flutter/material.dart';

class VerticalSpace15 extends StatelessWidget {
  const VerticalSpace15({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: 28.0.hp,
    );
  }
}
