import 'package:bhive_task/core/core.dart';
import 'package:flutter/material.dart';

class VerticalSpace1 extends StatelessWidget {
  const VerticalSpace1({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: 2.0.hp,
    );
  }
}
